#include<stdio.h>
int main()
{
    char romanval[1000];
    int i=0;
    long int num=0;
    printf("Enter roamn numb in capital=");
    scanf("%s", romanval);
    while(romanval[i])
    {
        if(digit (romanval[i]) 2)
        {
            if(digit(romanval[i])=digit(romanval[i+1]))
            number=number+
        }
    }
}